grey = off

blur = off
    blur_rate = 30

dilate = off

rotate = off
    rotate_angle = 50

resize = off
    resize_width =
    resize_height =

addText = off
    text =
    text_size =
    position_x =
    position_y =
    color =

watercolor = off